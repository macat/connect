en:
  dashboards:
    show:
      connect: "Connect"
      disconnect: "Disconnect"
      edit: "Edit"
      import_now: "Import now"
      sign_out: "Sign out"

  date:
    formats:
      default:
        "%m/%d/%Y"
      with_weekday:
        "%a %m/%d/%y"

  flashes:
    sign_in_failed: "Failed to sign in"
    sign_in_required: "Sign in with Namely"
    sign_in_succeeded: "Welcome, %{name}"
    signed_out: "Signed out"

  helpers:
    submit:
      jobvite_connection:
        update: "Connect"
      namely_authentication:
        submit: "Sign in"

  homepages:
    show:
      sign_in:
        prompt: "Sign in using"

  jobvite_connections:
    edit:
      title: "Edit Jobvite connection"

  jobvite_import:
    status:
      candidates_imported:
        one: "%{count} candidate imported"
        other: "%{count} candidates imported"
      jobvite_error: "Jobvite error: %{message}"
      not_connected: "No connected Jobvite account"

  simple_form:
    labels:
      jobvite_connection:
        api_key: "API key"
        secret: "Secret"
      namely_authentication:
        subdomain: Subdomain
    placeholders:
      namely_authentication:
        subdomain: yourcompany

  time:
    formats:
      default:
        "%a, %b %-d, %Y at %r"
      date:
        "%b %-d, %Y"
      short:
        "%B %d"

  titles:
    application: Namely Connect
