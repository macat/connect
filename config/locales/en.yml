en:
  dashboards:
    show:
      connect: "Connect"
      edit: "Edit"
      sign_out: "Sign out"

  date:
    formats:
      default:
        "%m/%d/%Y"
      with_weekday:
        "%a %m/%d/%y"

  flashes:
    sign_in_failed: "Failed to sign in"
    sign_in_required: "Sign in with Namely"
    sign_in_succeeded: "Welcome, %{name}"
    signed_out: "Signed out"

  helpers:
    submit:
      jobvite_connection:
        update: "Connect"
      namely_authentication:
        submit: "Sign in through Namely"

  simple_form:
    labels:
      jobvite_connection:
        api_key: "API key"
        secret: "Secret"
      namely_authentication:
        subdomain: Subdomain

  time:
    formats:
      default:
        "%a, %b %-d, %Y at %r"
      date:
        "%b %-d, %Y"
      short:
        "%B %d"

  titles:
    application: Connect
