<%= simple_form_for :namely_authentication, method: :get, url: new_session_path, html: { class: "sign-in" } do |f| %>
  <%= t(".sign_in.prompt") %>
  <%= f.input :subdomain %>.namely.com
  <%= f.submit %>
<% end %>
