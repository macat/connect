<%= render "subnav" %>

<h1>iCIMS</h1>
<strong><%= @icims_imports_presenter.errors %></strong>

<% if @icims_imports_presenter.imported_candidates.blank? %>
  <strong><%= t(".empty_successful_imports") %></strong>
<% else %>
  <strong><%= t(".imported_successfully") %></strong>
  <table>
    <thead>
      <tr>
        <th><%= t(".employee_title") %></th>
        <th><%= t(".key_title") %></th>
        <th><%= t(".contact_number_title") %></th>
        <th><%= t(".email_address_title") %></th>
      </tr>
    </thead>
    <tbody>
      <%= render @icims_imports_presenter.imported_candidates %>
    </tbody>
  </table>
<% end %>
<hr />
<% if @icims_imports_presenter.not_imported_candidates.present? %>
  <strong><%= t(".not_imported_successfully") %></strong>
  <table>
    <thead>
      <tr>
        <th><%= t(".employee_title") %></th>
        <th><%= t(".key_title") %></th>
        <th><%= t(".contact_number_title") %></th>
        <th><%= t(".email_address_title") %></th>
        <th><%= t(".reason") %></th>
      </tr>
    </thead>
    <tbody>
      <% @icims_imports_presenter.not_imported_candidates.each do |candidate| %>
        <tr>
          <td>
            <%= candidate.name %>
          </td>
          <td>
            <%= candidate.id %>
          </td>
          <td>
            <%= candidate.contact_number %>
          </td>
          <td>
            <%= candidate.email %>
          </td>
          <td>
            <%= candidate.error %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>
