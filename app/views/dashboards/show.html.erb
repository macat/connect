<%= button_to t("dashboards.show.sign_out"), session_path, method: :delete %>

<ul>
  <li>
    Jobvite
    <% if @jobvite_connection.connected? %>
      <%= @jobvite_connection.api_key %>
      <%= link_to t("dashboards.show.edit"), edit_jobvite_connection_path %> |
      <%= link_to t("dashboards.show.disconnect"), jobvite_connection_path, method: :delete %>
    <% else %>
      <%= link_to t("dashboards.show.connect"), edit_jobvite_connection_path %>
    <% end %>
  </li>
</ul>
